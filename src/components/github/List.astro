---
import Button from '../global/Button.astro';
import CardHeader from '../work/CardHeader.astro';
---

<ul class='github-list'>
  <li>
    <CardHeader
      category='app'
      title='permalik primary'
      description='Music Banking and Streaming App'
    />
    <nav>
      <Button href='/'>
        <img src='/site.svg' alt='External link icon' />
      </Button>
      <Button href='/'>
        <img src='/code.svg' alt='Angle bracket icon' />
      </Button>
    </nav>
  </li>
  <li>
    <CardHeader
      category='app'
      title='permalik primary'
      description='Music Banking and Streaming App'
    />
    <nav>
      <Button href='/'>
        <img src='/site.svg' alt='External link icon' />
      </Button>
      <Button href='/'>
        <img src='/code.svg' alt='Angle bracket icon' />
      </Button>
    </nav>
  </li>
  <li>
    <CardHeader
      category='app'
      title='permalik primary'
      description='Music Banking and Streaming App'
    />
    <nav>
      <Button href='/'>
        <img src='/site.svg' alt='External link icon' />
      </Button>
      <Button href='/'>
        <img src='/code.svg' alt='Angle bracket icon' />
      </Button>
    </nav>
  </li>
  <!-- TODO: Swap this production component upon deploy TODO: -->
  <!-- {
    data.map((repo) => (
      <li>
        <h2>
          <span>
            {repo.description
              .split(':')
              .map((k) => k)
              .slice(0, -1)}
          </span>
          {repo.name.split('-').join(' ')}
        </h2>
        <p>
          {repo.description
            .split(':')
            .map((e) => e)
            .slice(1)}
        </p>
        <nav>
          <a href={repo.homepage}>
            <img src='/site.svg' alt='External link icon' />
          </a>
          <a href={repo.html_url}>
            <img src='/code.svg' alt='Angle bracket icon' />
          </a>
        </nav>
      </li>
    ))
  } -->
</ul>

<style>
  .github-list {
    margin: 0 0 1.5rem;

    & > li {
      display: flex;
      flex-flow: column nowrap;

      & > nav {
        display: flex;
        flex-flow: row nowrap;
        align-items: center;
      }
    }
    & > li:not(:last-child) {
      margin: 0 0 1.5rem;
    }
    img {
      width: 1.5rem;
      height: 1.5rem;
    }
  }
</style>
