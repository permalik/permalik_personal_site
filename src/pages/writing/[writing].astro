---
import WritingLayout from '../../layouts/WritingLayout.astro';
export function getStaticPaths() {
  return [{ params: { writing: 'betterment' } }];
}

const { title } = Astro.params;
---

<WritingLayout>
  <h1>{title}</h1>
</WritingLayout>
